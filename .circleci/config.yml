jobs:
  build:
    docker:
      - image: cimg/node:15.2.1
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: npm install
      - run:
          name: Build android
          command: cd android && ./gradlew assembleRelease
          